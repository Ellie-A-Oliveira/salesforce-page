export enum ButtonVariants {
    PRIMARY = 'primary',
    PRIMARY_INVERTED = 'primary-inverted',
    SUCCESS = 'success',
    SUCCESS_INVERTED = 'success-inverted',
}

export enum Sizes {
    SMALL = 'sm',
    MEDIUM = 'md',
    LARGE = 'lg',
}