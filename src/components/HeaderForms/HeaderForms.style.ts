import styled from "styled-components";
import { colors } from "../../styles/colors.style";

export const HeaderFormsStyled = styled.header`
    top: 0;
    background-color: ${colors.background.default};
    padding: .75rem 2rem;

    .logo {
        width: 80px;
        margin-right: 2rem;
    }

`;